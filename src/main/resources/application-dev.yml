spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:volunteer_db}
    username: ${DB_USERNAME:volunteer_user}
    password: ${DB_PASSWORD:volunteer_pass}
  jpa:
    show-sql: true

logging:
  level:
    com.volunteer.management: DEBUG
